<% actions = capture do %>
  <%= link_to screens_path(site_id: @site.id), class: "rounded-md bg-white px-2.5 py-1.5 text-sm font-semibold text-gray-900 shadow-xs ring-1 ring-gray-300 ring-inset hover:bg-gray-50 inline-flex items-center gap-1.5" do %>
    Manage screens
  <% end %>
  <%= link_to edit_site_path(@site), class: "rounded-md bg-white px-2.5 py-1.5 text-sm font-semibold text-gray-900 shadow-xs ring-1 ring-gray-300 ring-inset hover:bg-gray-50 inline-flex items-center gap-1.5" do %>
    Edit site
  <% end %>
<% end %>
<%= render "app/shared/page_header", title: @site.name, subtitle: "Site", actions: actions, back_link: { label: "← Sites", path: sites_path } %>

<% tone = %w[#2f6bff #0fb5a6 #7c5cff #e8990f #1f9d57 #e5484d][@site.id % 6] %>
<%# Site photo or tinted hero %>
<div class="rounded-xl relative overflow-hidden mb-5" style="height: 170px; background: <%= tone %>18;">
  <% if @site.photo.attached? %>
    <%= image_tag @site.photo, class: "absolute inset-0 w-full h-full object-cover" %>
    <div class="absolute inset-0" style="background: linear-gradient(to top, rgba(0,0,0,0.3), transparent);"></div>
  <% else %>
    <div class="absolute inset-0" style="background: linear-gradient(135deg, <%= tone %>30, transparent);"></div>
  <% end %>
  <span class="absolute top-4 left-4 inline-flex items-center rounded-full bg-gray-900/8 px-2 py-0.5 text-xs font-medium text-gray-600 ring-1 ring-gray-500/10 ring-inset backdrop-blur-sm">Brokerage</span>
  <% if @site.address.present? %>
    <div class="absolute bottom-4 left-4">
      <p class="text-sm flex items-center gap-1.5 <%= @site.photo.attached? ? 'text-white' : 'text-gray-600' %>">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" class="size-3.5 shrink-0"><path stroke-linecap="round" stroke-linejoin="round" d="M15 10.5a3 3 0 11-6 0 3 3 0 016 0z"/><path stroke-linecap="round" stroke-linejoin="round" d="M19.5 10.5c0 7.142-7.5 11.25-7.5 11.25S4.5 17.642 4.5 10.5a7.5 7.5 0 1115 0z"/></svg>
        <%= @site.address %>
      </p>
    </div>
  <% end %>
</div>

<%# Screens at this site %>
<%= render "app/sites/screens_list", site: @site, screens: @site.screens.includes(screen_contents: :contentable).order(:name) %>

<%# Danger zone %>
<div class="mt-8 pt-4 border-t border-gray-200">
  <%= button_to "Delete site", @site, method: :delete,
      class: "rounded-md bg-white px-2.5 py-1.5 text-sm font-semibold text-red-600 shadow-xs ring-1 ring-red-300 ring-inset hover:bg-red-50",
      data: { turbo_confirm: "Are you sure you want to delete this site?" } %>
</div>
