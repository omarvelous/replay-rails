<%= render "app/shared/page_header", title: "Edit Brand Ad", back_link: { label: "← Ad", path: ad_path(@ad) } %>

<div data-controller="ad-builder" data-ad-builder-preview-url-value="<%= preview_ads_brand_ads_path %>">
  <%= form_with model: @ad, url: ads_brand_ad_path(@ad), method: :patch, data: { ad_builder_target: "form" } do |form| %>
    <%= render "app/ads/brand_ads/form", form: form %>
  <% end %>
</div>
