<%= render "app/shared/page_header", title: "Edit Collection Ad", back_link: { label: "← Ad", path: ad_path(@ad) } %>

<div data-controller="ad-builder" data-ad-builder-preview-url-value="<%= preview_ads_collection_ads_path %>">
  <%= form_with model: @ad, url: ads_collection_ad_path(@ad), method: :patch, data: { ad_builder_target: "form" } do |form| %>
    <%= render "app/ads/collection_ads/form", form: form %>
  <% end %>
</div>
