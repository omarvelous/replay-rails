<div class="font-extrabold tracking-tight leading-tight" style="font-size: var(--s-hero);"><%= ad.headline %></div>
<% if ad.body.present? %>
  <div style="font-size: var(--s-2xl); margin-top: var(--s-gap); color: var(--ad-text-muted);"><%= ad.body %></div>
<% end %>
