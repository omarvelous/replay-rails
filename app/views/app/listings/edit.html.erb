<%= render "app/shared/page_header", title: @listing.address, subtitle: "Edit listing", back_link: { label: "← Listing", path: listing_path(@listing) } %>

<div class="rounded-lg bg-white shadow-sm ring-1 ring-gray-900/5 p-5">
  <%= render "form", listing: @listing %>
</div>
