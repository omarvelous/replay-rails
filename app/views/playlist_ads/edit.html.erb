<%= turbo_frame_tag "modal" do %>
  <h3 class="font-bold text-lg mb-4">Edit Playlist Ad</h3>
  <%= render "form", playlist_ad: @playlist_ad, url: polymorphic_path([parent, @playlist_ad]) %>
<% end %>
