<%= turbo_frame_tag "listing_agents" do %>
  <div class="flex justify-between items-center mb-3 mt-8">
    <h2 class="font-bold text-2xl">Listings</h2>
    <%= link_to "Add Listing", new_agent_listing_agent_path(parent), class: "btn btn-primary btn-sm", data: { turbo_frame: "modal" } %>
  </div>

  <% if @listing_agents.any? %>
    <div class="overflow-x-auto">
      <table class="table">
        <thead>
          <tr>
            <th>Listing</th>
            <th>Role</th>
            <th></th>
          </tr>
        </thead>
        <tbody id="listing_agents_rows">
          <% @listing_agents.each do |la| %>
            <%= render "agents/listing_agents/listing_agent", la: la %>
          <% end %>
        </tbody>
      </table>
    </div>
  <% else %>
    <p class="text-base-content/60">No listings assigned to this agent yet.</p>
  <% end %>
<% end %>
