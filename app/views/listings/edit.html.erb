<%= turbo_frame_tag "modal" do %>
  <h3 class="font-bold text-lg">Edit listing</h3>
  <p class="text-sm text-base-content/50 mb-4">Update the property record used across RePlay signage.</p>
  <%= render "form", listing: @listing %>
<% end %>
