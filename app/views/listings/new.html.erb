<%= turbo_frame_tag "modal" do %>
  <h3 class="font-bold text-lg">New listing</h3>
  <p class="text-sm text-gray-500 mb-4">Add a property manually, or sync your MLS to import in bulk.</p>
  <%= render "form", listing: @listing %>
<% end %>
