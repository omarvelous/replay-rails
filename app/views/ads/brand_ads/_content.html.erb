<div class="text-4xl font-extrabold tracking-tight leading-tight"><%= ad.headline %></div>
<% if ad.body.present? %>
  <div class="text-base mt-3" style="color: var(--ad-text-muted);"><%= ad.body %></div>
<% end %>
