<% content_for(:page_title) { @ad.headline } %>
<% content_for(:page_subtitle) { "Edit ad" } %>

<%= link_to ad_path(@ad), class: "inline-flex items-center gap-1 text-sm font-semibold text-gray-500 hover:text-gray-900 mb-4" do %>
  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="size-4"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 19.5L8.25 12l7.5-7.5"/></svg>
  Back to ad
<% end %>

<%= render "form", ad: @ad %>
