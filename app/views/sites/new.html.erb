<%= turbo_frame_tag "modal" do %>
  <h3 class="font-bold text-lg">New site</h3>
  <p class="text-sm text-base-content/50 mb-4">Add a new location to your network.</p>
  <%= render "form", site: @site %>
<% end %>
